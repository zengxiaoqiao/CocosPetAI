package com.cocos.game;

/**
 * Demo ASR activity placeholder.
 *
 * The original SparkChain demo activity depends on demo-only UI resources and helper
 * classes. Runtime ASR integration for this project lives in {@link IflytekAsrEngine}
 * and is exposed through {@link AppActivity}'s existing JS bridge methods.
 */
final class asrActivity {
    private asrActivity() {
    }
}
